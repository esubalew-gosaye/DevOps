<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('partials/header', {title: "Details"}) %>
  <style>
    
  </style>
</head>

<body>
  <!-- Navigation Bar -->
  <%- include('partials/navbar') %> 

  <div class="container mt-4 w-50">
    <h2>Movie Details</h2>
    <div class="row">
      <div class="col-md-4 d-flex align-items-center justify-content-center">
        <img src="/movie_cover/<%= result.cover %>" alt="Movie Cover" class="img-fluid movie-cover" style="max-width: 100%; max-height: 300px;">
      </div>
      <div class="col-md-8">
        <h3><%= result.name %></h3>
        <p><strong>name:</strong> <%= result.name %></p>
        <p><strong>Type:</strong> <%= result.type %></p>
        <p><strong>Year:</strong> <%= result.year%></p>
      </div>
    </div>
  </div>
    </div>
    
  <!-- Footer -->
  <%- include('partials/footer') %> 
  </body>
  
  </html> 